   ###############################################################################
   # Define option defaults and initialize undefined variables
   ###############################################################################

set flow_defaults(rundir) .
set flow_defaults(script_dir) FF
set flow_defaults(plug_dir) PLUG
set flow_defaults(rpt_dir) RPT
set flow_defaults(dbs_dir) DBS
set flow_defaults(log_dir) LOG
set flow_defaults(html_dir) HTML
set flow_defaults(vpath) make
